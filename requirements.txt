# Runtime dependencies for the DEPLOYED showcase Space (Lane 3).
# CI does NOT install this file — it's Lane 2 and deliberately torch-free (see
# requirements-dev.txt, which lists the light app deps explicitly). The HF Space
# installs this, including the ML runtime the Live Inference page needs.
# Pinned: the Space rebuilds from scratch on every deploy, so an unpinned
# `streamlit` would silently pull whatever is latest — and use_container_width
# (used across the app, now migrated to width="stretch") is slated for removal.
# Pin to the version local dev + this migration were verified against.
streamlit==1.59.2
pandas
# Analytics page — in-process columnar queries over the committed Parquet table.
duckdb
pyarrow
# Live Inference page (Space): base Qwen2.5-0.5B + the committed LoRA adapter on
# CPU. Heavy (~GB build, slow cold start on a free CPU Space) but real.
torch
transformers
peft
