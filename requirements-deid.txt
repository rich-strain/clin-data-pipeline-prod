# Stage 2 free-text de-identification (LOCAL ONLY — deliberately NOT installed
# in CI). Running `run_stage2.py` needs Microsoft Presidio + a spaCy model; the
# de-identified notes + recall report are committed, and CI validates those
# committed artifacts (leakage == 0, recall report) without re-running the model.
#
# Install:  pip install -r requirements-deid.txt
presidio-analyzer
en_core_web_lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
