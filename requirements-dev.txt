# Dev/CI tooling (Lane 2 — deliberately torch-free: CI never trains or runs live
# inference). Lists the light app deps explicitly rather than `-r requirements.txt`
# so CI doesn't pull the heavy ML runtime that file carries for the Space. Keep
# the light app deps here in sync with requirements.txt.
streamlit
pandas
duckdb
pyarrow
# Tooling.
ruff
mypy
pytest
# Build-time FHIR R4 structural validation (Stage 0/1). Not needed by the app
# at runtime (it reads the committed validation report), so it's dev-only.
fhir.resources
# Stage 4 data-quality gate (Pandera). Runs on committed curated artifacts
# (free, no API), so CI re-runs it; the app only reads the committed gate report.
pandera
